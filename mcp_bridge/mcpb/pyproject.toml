[project]
name = "toolkitpalm-mcp-bridge"
version = "1.0.0"
description = "Puente MCP para conectar Claude con QGIS (plugin ToolkitPalm)"
requires-python = ">=3.10"
dependencies = [
    "mcp>=2.0.0",
]
